function initializeQuiz(contextData) {
    // Utilisez contextData à l'intérieur de cette fonction
    console.log(contextData);
    
    alert("hello!");
    // Le reste de votre code JavaScript ici
}